{
    "template": "warning_edit",
    "description": "Hide delete button if custom warning",
    "execution_order": 10,
    "enabled": true,
    "action": "str_replace",
    "find": "$warning.isUpdate()",
    "replace": "$warning.isUpdate() && {{ $warning.is_custom === false }}"
}